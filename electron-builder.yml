appId: com.scheduleassistant.app
productName: Schedule Assistant
directories:
  buildResources: build
  output: release
files:
  - dist-electron
  - dist
protocols:
  - name: Schedule Assistant
    schemes:
      - scheduleapp
win:
  target:
    - dir
  signAndEditExecutable: false
mac:
  target:
    - dmg
  artifactName: ${productName}-${version}.${ext}
linux:
  target:
    - AppImage
  artifactName: ${productName}-${version}.${ext}
